import React from "react";

// Import from index file to improve module resolution
import { MediaSimulationWizard } from "../components";

export const MediaSimulationContainer: React.FC = () => {
  return <MediaSimulationWizard />;
};
